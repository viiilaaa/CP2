- name: SUbir imágenes a ACR
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars_nc.yml
  tasks:
    - name: Incluir el rol de configuración de ACR
      ansible.builtin.include_role:
        name: AKS_conf
