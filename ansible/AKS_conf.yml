- name: SUbir imágenes a ACR
  hosts: localhost
  gather_facts: false
  vars_files:
    - secrets.yml
  tasks:
    - name: Incluir el rol de configuración de ACR
      ansible.builtin.include_role:
        name: aks_deploy
