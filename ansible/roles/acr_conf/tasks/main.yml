---
# tasks file for acr_conf

- name: Iniciar sesión en ACR
  containers.podman.podman_login:
    registry: "{{ acr_login_server }}"
    username: "{{ acr_admin_username }}"
    password: "{{ acr_admin_password }}"

- name: Descargar, etiquetar y subir imágenes a ACR
  ansible.builtin.include_tasks: PullPushImage.yml
  loop: "{{ images_to_process }}"
  loop_control:
    loop_var: item

- name: Cerrar sesión de ACR
  containers.podman.podman_logout:
    registry: "{{ acr_login_server }}"
