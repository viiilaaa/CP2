apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ prefix }}-pvc"
  namespace: "{{ namespc }}"
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi